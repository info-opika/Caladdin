# Process Rules

- Spec changes require updating `CALADDIN_FULL_APPLICATION_SPEC.md` or an explicit DECISIONS.md entry.
- One intent per orchestrator invocation.
- Destructive operations require confirmation unless `_skipConfirmationGate` after approval.
- Off-topic input: warm redirect, not RESOLVE_MANUAL.
